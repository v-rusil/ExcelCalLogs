import { LogProvider, M365TokenProvider, TelemetryReporter, UserInteraction } from "@microsoft/teamsfx-api";
import { DebugAction } from "./common";
export interface SSODebugArgs {
    objectId?: string;
    clientId?: string;
    clientSecret?: string;
    accessAsUserScopeId?: string;
}
export declare class SSODebugHandler {
    private readonly projectPath;
    private args;
    private readonly m365TokenProvider;
    private readonly logger;
    private readonly telemetry;
    private readonly ui;
    private projectSettingsV3?;
    private cryptoProvider?;
    private envInfoV3?;
    constructor(projectPath: string, args: SSODebugArgs, m365TokenProvider: M365TokenProvider, logger: LogProvider, telemetry: TelemetryReporter, ui: UserInteraction);
    getActions(): DebugAction[];
    private validateArgs;
    private registerAAD;
    private configureAAD;
    private buildAndSaveAADManifest;
    private saveStates;
    private setEnvs;
    private constructPluginContext;
}
//# sourceMappingURL=sso.d.ts.map