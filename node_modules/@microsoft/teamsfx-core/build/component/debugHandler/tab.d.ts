import { LogProvider, M365TokenProvider, TelemetryReporter, UserInteraction } from "@microsoft/teamsfx-api";
import { DebugAction } from "./common";
export interface TabDebugArgs {
    baseUrl?: string;
}
export declare class TabDebugHandler {
    private readonly projectPath;
    private args;
    private readonly m365TokenProvider;
    private readonly logger;
    private readonly telemetry;
    private readonly ui;
    private projectSettingsV3?;
    private cryptoProvider?;
    private envInfoV3?;
    constructor(projectPath: string, args: TabDebugArgs, m365TokenProvider: M365TokenProvider, logger: LogProvider, telemetry: TelemetryReporter, ui: UserInteraction);
    getActions(): DebugAction[];
    private validateArgs;
    private saveStates;
    private setEnvs;
}
//# sourceMappingURL=tab.d.ts.map