import { TelemetryReporter } from "@microsoft/teamsfx-api";
interface DeploymentInfo {
    lastBuildTime?: string;
    lastDeployTime?: string;
}
export declare class FrontendDeployment {
    static needBuild(componentPath: string, envName: string, telemetryReporter?: TelemetryReporter): Promise<boolean>;
    static needDeploy(componentPath: string, envName: string): Promise<boolean>;
    private static hasUpdatedContent;
    private static getDeploymentInfo;
    private static getLastBuildTime;
    private static getLastDeploymentTime;
    static saveDeploymentInfo(componentPath: string, envName: string, deploymentInfo: DeploymentInfo): Promise<void>;
}
export {};
//# sourceMappingURL=deploy.d.ts.map