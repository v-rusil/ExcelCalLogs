"use strict";
// Copyright (c) Microsoft Corporation.
// Licensed under the MIT license.
Object.defineProperty(exports, "__esModule", { value: true });
exports.RemoteBotRegistration = void 0;
const teamsfx_api_1 = require("@microsoft/teamsfx-api");
const botRegistration_1 = require("./botRegistration");
class RemoteBotRegistration extends botRegistration_1.BotRegistration {
    async createBotRegistration(m365TokenProvider, aadDisplayName, botName, botConfig, logProvider, botAuthType = botRegistration_1.BotAuthType.AADApp) {
        const botAadRes = await super.createBotAadApp(m365TokenProvider, aadDisplayName, botConfig);
        if (botAadRes.isErr()) {
            return teamsfx_api_1.err(botAadRes.error);
        }
        // Didn't provision Azure bot service because it's handled by arm/bicep snippets.
        return teamsfx_api_1.ok(botAadRes.value);
    }
    async createOrUpdateBotRegistration(m365TokenProvider, botRegistration) {
        // Do nothing because it's handled by arm/bicep snippets.
        return teamsfx_api_1.ok(false);
    }
    async updateMessageEndpoint(m365TokenProvider, botId, endpoint) {
        // Do nothing because it's handled by arm/bicep snippets.
        return teamsfx_api_1.ok(undefined);
    }
}
exports.RemoteBotRegistration = RemoteBotRegistration;
//# sourceMappingURL=remoteBotRegistration.js.map