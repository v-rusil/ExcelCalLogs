import { AxiosInstance } from "axios";
import { IAadInfo, IPasswordCredential, IServicePrincipal } from "../interfaces/IAadResource";
import { LogProvider, TelemetryReporter } from "@microsoft/teamsfx-api";
export declare class AadService {
    private readonly logger;
    private readonly telemetryReporter;
    private readonly axios;
    private readonly maxRetries;
    constructor(axios: AxiosInstance, telemetryReporter?: TelemetryReporter, logger?: LogProvider, maxRetries?: number);
    createAad(aadName: string): Promise<IAadInfo>;
    addSecret(objectId: string, displayName: string): Promise<IPasswordCredential>;
    getAad(objectId: string): Promise<IAadInfo | undefined>;
    updateAad(objectId: string, data: IAadInfo): Promise<void>;
    getServicePrincipals(appId: string): Promise<IServicePrincipal[]>;
    createServicePrincipalIfNotExists(appId: string): Promise<void>;
    private execute;
}
//# sourceMappingURL=aadService.d.ts.map