import { LogProvider, TelemetryReporter } from "@microsoft/teamsfx-api";
import { IAadPluginConfig, IApimPluginConfig } from "../config";
import { AadService } from "../services/aadService";
import { Lazy } from "../utils/commonUtils";
export declare class AadManager {
    private readonly logger;
    private readonly telemetryReporter;
    private readonly lazyAadService;
    constructor(lazyAadService: Lazy<AadService>, telemetryReporter?: TelemetryReporter, logger?: LogProvider);
    provision(apimPluginConfig: IApimPluginConfig, appName: string): Promise<void>;
    postProvision(apimPluginConfig: IApimPluginConfig, aadPluginConfig: IAadPluginConfig, redirectUris: string[]): Promise<void>;
    private refreshEnableIdTokenIssuance;
    private refreshRedirectUri;
    private refreshRequiredResourceAccess;
}
//# sourceMappingURL=aadManager.d.ts.map