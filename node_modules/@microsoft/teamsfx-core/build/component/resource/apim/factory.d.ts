import { AzureAccountProvider, EnvInfo, LogProvider, M365TokenProvider, Platform, TelemetryReporter, v3 } from "@microsoft/teamsfx-api";
import { ISolutionConfig } from "./config";
import { AadManager } from "./managers/aadManager";
import { ApimManager } from "./managers/apimManager";
import { IQuestionManager } from "./managers/questionManager";
import { ScaffoldManager } from "./managers/scaffoldManager";
import { TeamsAppAadManager } from "./managers/teamsAppAadManager";
import { AadService } from "./services/aadService";
import { ApimService } from "./services/apimService";
export declare class Factory {
    static buildApimManager(envInfo: EnvInfo | v3.EnvInfoV3, telemetryReporter?: TelemetryReporter, azureAccountProvider?: AzureAccountProvider, logProvider?: LogProvider): Promise<ApimManager>;
    static buildAadManager(m365TokenProvider?: M365TokenProvider, telemetryReporter?: TelemetryReporter, logProvider?: LogProvider): Promise<AadManager>;
    static buildTeamsAppAadManager(m365TokenProvider?: M365TokenProvider, telemetryReporter?: TelemetryReporter, logProvider?: LogProvider): Promise<TeamsAppAadManager>;
    static buildScaffoldManager(telemetryReporter?: TelemetryReporter, logProvider?: LogProvider): Promise<ScaffoldManager>;
    static buildQuestionManager(platform: Platform, envInfo: EnvInfo | v3.EnvInfoV3, azureAccountProvider?: AzureAccountProvider, telemetryReporter?: TelemetryReporter, logProvider?: LogProvider): Promise<IQuestionManager>;
    static buildApimService(solutionConfig: ISolutionConfig, azureAccountProvider: AzureAccountProvider | undefined, telemetryReporter?: TelemetryReporter, logger?: LogProvider): Promise<ApimService>;
    static buildAadService(m365TokenProvider?: M365TokenProvider, telemetryReporter?: TelemetryReporter, logger?: LogProvider): Promise<AadService>;
}
//# sourceMappingURL=factory.d.ts.map