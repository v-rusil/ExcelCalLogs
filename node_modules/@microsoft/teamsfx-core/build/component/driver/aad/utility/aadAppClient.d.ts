import { M365TokenProvider } from "@microsoft/teamsfx-api";
import { AADApplication } from "../../../resource/aadApp/interfaces/AADApplication";
import { AADManifest } from "../../../resource/aadApp/interfaces/AADManifest";
export declare class AadAppClient {
    private readonly retryNumber;
    private readonly tokenProvider;
    private readonly axios;
    constructor(m365TokenProvider: M365TokenProvider);
    createAadApp(displayName: string): Promise<AADApplication>;
    generateClientSecret(objectId: string): Promise<string>;
    updateAadApp(manifest: AADManifest): Promise<void>;
    private is404Error;
    private is400Error;
}
//# sourceMappingURL=aadAppClient.d.ts.map